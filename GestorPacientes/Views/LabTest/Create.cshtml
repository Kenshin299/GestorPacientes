@model GestorPacientes.Core.Application.ViewModels.LabTest.SaveLabTestViewModel

<div class="d-flex justify-content-center align-items-center">
    <form asp-action="Create" method="post" class="col-md-6">
        <div class="form-group">
            <label for="Name">Name</label>
            <input type="text" class="form-control" id="Name" name="Name" placeholder="Name" value="@Model.Name" required>
        </div>
        <button type="submit" class="btn btn-primary mt-2">Create Lab Test</button>
    </form>
</div>
